# 2026-08-17

## Office L0 动态行人 SCAN 闭环验收链落地

- 日期：2026-08-17
- 任务：`scan-planner-reproduction`，Office L0 + 8 个 NVIDIA 官方行人 + Lite3 V12 `model_149999` + ROS 2 Foxy 项目移植 SCAN + TCP bridge + Isaac PhysX 闭环。
- 行为改动：
  - 因果审计从“有检测/有新轨迹”收紧为同一人的 LiDAR/depth 检测、剩余连续 B-spline 邻近、实际 `/grid_map/occupancy_inflate` 占据及同目标 B-spline 几何变化的完整证据链。
  - 时间对齐改为使用 body-pose receipt 夹点的 simulator stamp 插值，避免在低于实时速率的 Isaac 中用墙钟误判规划因果顺序。
  - 重复性门禁新增有效输入 SHA-256 和归一化 run identity SHA-256 一致性检查；路线新增显式走廊拐点，并用测试锁定 route/planner/acceptance 三层航点一致。
  - Review overlay 现在显示实际 SCAN inflated occupancy、规划/实际轨迹及 `NEW SCAN B-SPLINE`；GT 行人路线明确标记为 `evaluation only`。相机低内容检测及追踪视角回退只影响展示，不改变规划、物理或传感器。
- 验证：`office_crowd_codex_candidate38` 和 `candidate39` 在相同有效输入下分别得到 5/1 次严格因果重规划，目标误差 `0.1086/0.1308 m`，最小保守行人间隔 `0.8543/0.6420 m`，均为零非足接触、零 watchdog/protocol error；有效输入和归一化 identity 哈希分别一致。`candidate35` 保留为 FAIL 诊断证据。三套运行已从 5070 Ti 同步回本地并核对关键哈希，视频可解码。本地测试为 `124 passed` + `5 subtests passed`，场景工具 `7 passed`，JSON/JSONL/YAML/Trellis 校验通过。
- 影响路径：`integration/lite3_sim_bridge/`、`integration/scan_planner_foxy_ws/`、`.pipeline/experiments/2026-08-17_office_l0_scan_crowd/`、`.trellis/tasks/07-23-scan-planner-reproduction/`、`.trellis/spec/backend/quality-guidelines.md`。
- 验收边界：AC54 自动门禁已通过；AC55 仍未勾选。Lite3 在白色地面上对比度偏低，且部分镜头靠近黑色家具，最终视觉可接受性必须由 Dr Sun 人工判定；该结果不是上游 SCAN 完整复现、真实人群安全或社会导航验证。
- 提交：`f50f750 feat(sim): validate Office crowd SCAN loop`。
- 后续意义：未来扩展场景或更换规划器时，应复用这套“实际感知—实际占据—连续轨迹变化—同输入身份”验收契约，不用展示叠加物或单次 PASS 代替因果与重复性证据。
