# 近期改动

> 最后更新：2026-08-31 PDT

## 2026-08-31

- Dr Sun 授权并完成当前 v2.0.1 的 60 秒平地 Office 视频 dry run。
- `dryrun01` 保留为包装中断 FAIL；修正 conda 激活后错误回指历史 bridge 的
  `PYTHONPATH`，以脱离会话方式运行 `dryrun02`。
- `dryrun02` 通过 601/601 双云、终点/两秒停止、八行人完整时长、原生 RViz
  和视频门；AC55 仍待 Dr Sun。
- 保留 327,133,479-byte 4K 母版与 62,373,833-byte 标准版；第一份
  1920x540 压缩因 SSIM 0.939357 保持 FAIL，第二份 1280x360 直传版为
  9,176,726 bytes、SSIM 0.964990，自动与本地抽检均通过。
- 为两个 60 秒尝试分别生成 366/398 条远端递归哈希清单，并回收声明的本地
  审核子集；未把远端原始大文件包装成完整本地 parity。

## 2026-08-21

- 完成 v2.0.1 只读验收，未改实现。新 profile 平地预检有条件通过，总体版本不通过，任务保持 `in_progress`。
- 重跑本地 targeted `86` 项和全量 `153` 项（`2` skip）；复核 Foxy targeted `25` 项与 `4` 个 CTest，整包因缺 `torch` 未全过。
- 核对 101/101 双点云、Go2 五值、`9,515,787` bytes 传输视频解码/哈希及 1/5/9 秒抽帧。
- 频率边界：仿真时间约 `10 Hz`，墙钟约 `0.539 Hz`；Decay 0 仅持有最新帧。
- 发现四个待修复边界：legacy 默认 profile 的 RViz 点云 topic 回归；130 个远端实体中 63 个本地缺失、1 个不同；`AGENTS.md` / parent task meta 状态源漂移；`connect_timeout` 声明未实际生效。

## 2026-08-20

- 实现 `office-v2.0.1-go2-geometry-preflight` / `upstream_go2_reference`：同次 MID-360 扫描生成 raw 可视化云与 planner 规划云，两者同 scan ID/时间戳/坐标系。
- 接入 Go2 官方五个碰撞几何值与 Path z + `body_height` 高度合约，保持 Lite3 `0.50 m/s`、Office 范围、策略、控制与场景冻结。
- 平地 `preflight01` 失败证据保留；`preflight02` + `retry02` 完成 101/101 双云审计。

## 2026-08-19

- 接入可追溯 Livox MID-360 pattern：0.1--40 m、10 Hz、20,000 rays/scan 与 same-step pose/cloud/stamp；只验证几何传感和 Office 短链路。
- 冻结 `office-dualview-v1.0.0` 并建立 revision ledger/validator；任何短预检均未升级为 accepted/formal。
