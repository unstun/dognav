# 未关闭决策

> 最后更新：2026-08-31 PDT

## 当前验收决策

- [ ] **修复 legacy 点云回归**：旧 `legacy_planner_v1` 只发 `/quad_0/cloud`，共享 RViz/voxel 听 `/quad_0/cloud_raw`；需为旧 profile 恢复可见点云，并为新旧 profile 增加回归门。
- [ ] **回收并核对完整运行实体**：当前远端 manifest 130 个文件中，本地只有 66 个匹配、63 个缺失、1 个视频不同；全量递归 SHA-256 parity 未通过。
- [ ] **同步项目状态源**：`AGENTS.md` Current Boundary 和 parent task meta 仍是旧 revision；需与当前 v2.0.1 证据和 `in_progress` 结论对齐。
- [ ] **修复建连超时参数**：将声明的 `connect_timeout=1 s` 真正传给客户端，与 `receive_timeout=10 s` 分离，并增加断言。
- [ ] **非平地短预检授权（Dr Sun 持有）**：只有再次明确批准后才能运行；平地 profile 有条件通过不构成复杂地形能力结论。
- [ ] **Foxy 完整 bridge package 环境门**：当前镜像缺 `torch`；需决定是否补齐钉住依赖后重跑全包。
- [ ] **新 AC54 / AC55 与正式候选**：均需 Dr Sun 后续明确授权；在此前 `accepted_revision` / `formal_candidate` 保持为空。
- [ ] **Dr Sun 人工审看**：60.04 秒双视角直传版已完成自动门和本地每 10 秒
  抽样，仍需 Dr Sun 决定 AC55；本地抽检不代替正式人工接受。

## 其他开放事项

- [ ] Lite3 LiDAR 总装下一证据入口：厂商完整总装 CAD，或真机实测/扫描孔距、基准、位置、接触面和螺钉链。
- [ ] 历史森林动态人物、原生三维体素与 Mac Humble RViz 仍是独立待复核证据。

## 已关闭

- [x] 新 profile 同 scan 双点云、Go2 五个几何参数及三维 Path 高度合约已实现并通过当前单元/针对性门；不等于整体版本验收。
- [x] 10.04 秒平地短回归已保留失败 run 并得到成功 run 的 101/101 双云审计。
- [x] 小于 10,000,000 bytes 的传输版视频已完整解码并核对哈希；未覆盖母版。
- [x] 60 秒平地 `dryrun02` 已通过声明的终点、停止、行人、双云、RViz 和
  交付门；这不关闭非平地、新 AC54、AC55 或正式候选决策。
