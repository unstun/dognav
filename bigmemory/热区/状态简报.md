# 项目状态简报

> 最后更新：2026-08-21 PDT

## 当前验收结论
- **总体版本验收不通过**；`08-20-v2-0-1-cloud-traversability` 保持 `in_progress`。
- 新 `office-v2.0.1-go2-geometry-preflight` / `upstream_go2_reference` 只在 10.04 秒平地 Office 短预检下**有条件通过**；平地成功不等于非平地或复杂地形能力。
- 本次是只读验收，未修改实现代码。

## 当前阶段
- 实验 + 分析；当前工作是修复验收中发现的回归与归档缺口，不得提升为正式候选。

## 已核实证据
- 新 profile 把同一 scan 的 raw 云 `/quad_0/cloud_raw` 给 RViz，planner 云 `/quad_0/cloud` 给 SCAN；101/101 同帧配对，scan ID 1--101，raw/planner/ground 点数守恒。
- Go2 上游五个几何值核对为 `0.25/0.18/0.40/0.10/0.10 m`；Lite3 `0.50 m/s` 和 Office horizon `8 m` 未随上游漂移。
- 本地 targeted `86` 项和全量 `153` 项通过，`2` 项按环境跳过；台账、编译、shell 和 diff 检查通过。归档 Foxy targeted `25` 项与 `4` 个 CTest 通过；整包因镜像缺 `torch` 未全过。
- 传输版视频为 `9,515,787` bytes，H.264，3840x1080，10.04 秒，完整解码通过，SHA-256 `2b6009e80ba12c109fd6aee7fadfa2eb190e4d8e0369ac495d02152c246400be`；1/5/9 秒抽样可见点云。
- 点云仿真时间频率为 `10.0000002 Hz`；慢仿真下墙钟只有约 `0.539 Hz`。RViz Decay 0 只保留最新帧以保持视觉连续，不代表墙钟 10 Hz 刷新。

## 当前阻断

- 旧默认 `legacy_planner_v1` 只发 `/quad_0/cloud`，共享 RViz/voxel 却已改听 `/quad_0/cloud_raw`，旧模式可能无点云；需修复并做新旧 profile 回归。
- 远端 manifest 有 130 个文件，当前本地实体比对只有 66 个匹配、63 个缺失、1 个 `closed_loop.mp4` 不同；完整实体回收未完成。
- `AGENTS.md` Current Boundary 和 parent task meta 仍停在旧版本，与热区/实验证据不一致，需同步状态源。
- 配置声明的 `connect_timeout=1 s` 未传入客户端，实际建连仍用 `receive_timeout=10 s`；属小缺陷。

## 未验证边界

- 非平地仿真、新 AC54、AC55、正式候选和真机均未执行；`accepted_revision` / `formal_candidate` 仍为 `null`。
- 旧失败 run 和旧 revision 仍是不可覆盖证据，不能用当前平地短预检替代。
