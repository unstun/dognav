# 项目状态简报

> 最后更新：2026-08-20 PDT

## 当前阶段

- 实验 + 分析；`08-20-v2-0-1-cloud-traversability` 已启动且保持 `in_progress`。

## 活跃任务

- `v2.0.1` 点云分流与 SCAN 几何接入：双点云、Go2 独立 profile 及三维 Path 高度合约已实现，待独立授权非平地短预检。
- `scan-planner-reproduction`：旧 `candidate38/39` 仅在原输入下通过 AC54；AC55 仍由 Dr Sun 持有。
- `07-24-lite3-pro-parametric-model`：factory Lite3 LiDAR 总装证据不足，继续冻结。

## 关键上下文

- revision 为 `office-v2.0.1-go2-geometry-preflight`，profile 为 `upstream_go2_reference`；首次 run ID 已作为失败证据保留。
- 同扫描 `/quad_0/cloud_raw` 保留地面供 RViz，`/quad_0/cloud` 过滤连续地面后供 SCAN；两者同 stamp/frame/scan ID。
- 仅借用 Go2 官方半径 `0.25`、偏移 `0.18`、机身高 `0.40`、z 上/下膨胀 `0.10/0.10` m；Lite3 速度 `0.50 m/s` 与 Office 范围未漂移。
- 本地 153 tests PASS；Foxy SCAN GTests 与 targeted bridge 25 tests PASS；完整 bridge package 因容器缺 `torch` 未全过。
- 平地 10.04 s `preflight01` 失败保留；`preflight02` 成功，后处理 `retry02` 恢复，双云审计 101/101。

## 未解决警告

- 非平地仿真尚未执行，须 Dr Sun 另行明确批准；当前不得宣称 Lite3 具备复杂地形能力。
- 未跑新 AC54、AC55、真机或正式候选；`accepted_revision` / `formal_candidate` 仍为 `null`。
- 旧 revision、失败 run、主/压缩视频及哈希都是不可覆盖历史证据。
