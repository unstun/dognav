# 项目状态简报

> 最后更新：2026-08-31 PDT

## 当前验收结论
- **总体版本仍未验收**；`08-20-v2-0-1-cloud-traversability` 保持 `in_progress`。
- 新 `office-v2.0.1-go2-geometry-preflight` / `upstream_go2_reference`
  已通过一次 60.03 秒平地 Office 完整时长 dry run 的声明门；该结果仍待 Dr
  Sun 人工审看，且平地成功不等于非平地或复杂地形能力。
- `accepted_revision` / `formal_candidate` 仍为空，AC55 仍由 Dr Sun 持有。

## 当前阶段
- 实验 + 分析；当前进入 60 秒视频人工审看，不得自动提升为正式候选。

## 已核实证据
- `office_v2_0_1_go2_geometry_dryrun02` 运行 60.029998658 秒，完成
  601/601 raw/planner 同帧配对；RViz 原始云在 1,501/1,501 视频帧中可见，
  最长空白为 0 帧。
- 终点 XY 误差 0.10591 m；连续两秒停止窗最大指令为 0，最大平面速度
  0.01166 m/s；八名行人的完整时长根运动/动画一致性门通过。
- Go2 上游五个几何值核对为 `0.25/0.18/0.40/0.10/0.10 m`；Lite3 `0.50 m/s` 和 Office horizon `8 m` 未随上游漂移。
- 4K 母版为 327,133,479 bytes、3840x1080、60.04 秒；标准版为
  62,373,833 bytes。直传版为 9,176,726 bytes、1280x360、60.04 秒，
  完整解码和 SSIM 0.964990 通过，SHA-256
  `308f8500634fc567413ab826d9782bdbd2dc239ec7e7b63fc9b3b75b1fcac0b5`。
- `dryrun01` 在完成仿真后因附着 SSH 通道关闭而中断包装，保持不可变 FAIL；
  `dryrun02` 使用脱离会话的执行方式完成，不覆盖第一次证据。
- 点云仿真时间频率为 `10.0000002 Hz`；慢仿真下墙钟只有约 `0.539 Hz`。RViz Decay 0 只保留最新帧以保持视觉连续，不代表墙钟 10 Hz 刷新。

## 当前阻断

- 旧默认 `legacy_planner_v1` 只发 `/quad_0/cloud`，共享 RViz/voxel 却已改听 `/quad_0/cloud_raw`，旧模式可能无点云；需修复并做新旧 profile 回归。
- 当前 60 秒成功 run 的远端完整树有 398 个 manifest 条目；本地仅回收声明的
  视频/审计子集，rosbag、原始事件流、体素快照和辅助视频仍留在执行副本，
  不宣称完整本地 parity。
- `AGENTS.md` Current Boundary 和 parent task meta 仍停在旧版本，与热区/实验证据不一致，需同步状态源。
- 配置声明的 `connect_timeout=1 s` 未传入客户端，实际建连仍用 `receive_timeout=10 s`；属小缺陷。

## 未验证边界

- 非平地仿真、新 AC54、AC55、正式候选和真机均未执行；`accepted_revision` / `formal_candidate` 仍为 `null`。
- 旧失败 run 和旧 revision 仍是不可覆盖证据，不能用当前平地短预检替代。
