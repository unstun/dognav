# 项目状态简报

> 最后更新：2026-08-20 PDT

## 当前阶段

- 实验 + 分析；`scan-planner-reproduction` 及子任务 `office-crowd-review-visual-r2` 均保持 `in_progress`，不得归档为完成。

## 活跃任务

- Office L0 人审修订：`office-r2.0.1-preflight` 的第 6 次 5070 Ti
  10.04 秒短预检已通过声明内自动门，等待 Dr Sun 审看传输版视频。
- `scan-planner-reproduction`：旧 `candidate38/39` 的 AC54 已通过，AC55 仍由 Dr Sun 持有。
- `07-24-lite3-pro-parametric-model`：factory Lite3 LiDAR 总装证据不足，模型与打印生成器保持冻结。

## 关键上下文

- `candidate38/39` 是不可变旧输入证据；其 AC54 PASS 不能沿用给 MID-360 新输入。
- MID-360 合约：Livox 有序 pattern、0.1--40 m、10 Hz、20,000 rays/scan，body/sensor/cloud/stamp 同 Isaac step；`smoke03` 与 Office 集成预检有证据。
- `office-dualview-v1.0.0` 仅冻结展示模板：高清全局第三视角 + 5070 Ti 同 run 原生 RViz。
- 当前 working revision 为 `office-r2.0.1-preflight`，父版本 r2.0.0；唯一
  change group 是 `golden_dualview_delivery_reliability`，展示/交付合约为
  `office-dualview-v1.0.1`。它只修 RViz 最新真云保留、live fail-closed
  审计和 transfer 打包，不改 MID-360/SCAN/策略/场景输入。
- `preflight06`：101 个 10 Hz 仿真时钟扫描，收到 96 个非空真云，覆盖率
  0.950495；仿真 gap 最大 0.2 秒，stamp 回退 0；视频 251/251 帧有云，
  最长空白 0。CRF26 传输版 11,543,483 B，相对 58,781,800 B 母版缩小
  80.36%，SSIM 0.974314，完整解码通过。
- `accepted_revision` / `formal_candidate` 仍为 `null`；AC54 未重跑，AC55
  仍待 Dr Sun。失败的 preflight01--05 与成功 run 均不可变保留。

## 未解决警告

- `preflight06` 仍只是 3840x1080、25 fps、251 帧、10.04 s 短预检；
  terminal goal/stop `SKIPPED`、完整时长行人比例 `NOT_GATED`，未到终点、
  未重跑新 AC54、未满足 AC55。
- 下一步仅等 Dr Sun 视觉判定；若认可，须再明确授权 full-duration dry runs 与 fresh formal candidate；若不认可，先建新 revision。
- `candidate38/39` 及所有 preflight 均不得覆盖、改名或追溯重标。
- 回答“当前做到哪里”时必须先核对主项目、全部 worktree/branch、Trellis、`.pipeline` 与热区；隐藏工作区中的更新不能视为已正确归档。
