---
paths: ["CLAUDE.md", "AGENTS.md", ".pipeline/**"]
---
# Git Push Permission Extension

After commits for `CLAUDE.md`, `AGENTS.md`, or `.pipeline/*` are complete, you may run `git push` directly without extra confirmation.
Why: on 2026-04-08, Dr Sun explicitly said workflow documents should not accumulate as local unpushed changes. This matches hard rule #6 but applies more broadly.
Exception: still NEVER force-push. For changes involving unpublished research decisions, ask Dr Sun briefly first.
