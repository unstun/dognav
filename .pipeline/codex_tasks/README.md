# External Task Packages

Store reviewable Codex, remote, or external CLI task packages here.

Each package must state:

- local repository source of truth;
- allowed files and actions;
- sync-back gate;
- validation command;
- expected artifacts;
- claim boundary.
