# source_references

外部代码、walking 基础仓库快照和论文代码索引放这里。

规则：

- `upstream/` 或 `external/` 子目录默认不进 git。
- 每个 reference 需要 `_meta/README.md` 或 `SOURCE_SNAPSHOT.yaml` 说明来源、commit、许可证和用途。
- 旧 walking 仓库只能作为来源参考，不能作为 nav 实验证据。
