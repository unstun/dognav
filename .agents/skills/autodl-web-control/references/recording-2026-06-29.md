# Record & Replay 证据：AutoDL 网页实例控制

来源：Record & Replay session `5D2719CE-EF99-4337-BE28-128E969B73AC`，录制时间是 2026-06-29。

用途：只作为稳定页面文字、实例行内容、按钮名和弹窗文字的证据。不要回放坐标。

录制中观察到的页面入口：

```text
https://www.autodl.com/console/instance/list
```

2026-06-29 后续实操中观察到的 Chrome / 账号入口：

```text
Chrome profile: 皓
Chrome profile directory: Profile 1
AutoDL 控制台账号显示: 炼丹师7151
实例列表 URL 可带随机刷新参数，例如：
https://www.autodl.com/console/instance/list?_random_=1782733860467
```

录制中观察到的运行态目标行：

```text
实例: 内蒙B区 / 094机 2c89409aa0-5dc36344
状态: 运行中
规格: RTX 4090 * 1卡
付费方式: 按量计费
释放时间/停机时间: 关机15天后释放
快捷工具: JupyterLab / AutoPanel / 实例监控 / 自定义服务
操作: 关机 / 更多
```

录制中观察到的关机第一步：

```text
Click AXButton title: 关机
```

录制中观察到的关机确认弹窗：

```text
确认关机吗？
实例：2c89409aa0-5dc36344
按量计费的实例关机后GPU将会释放，再次开机时可能面临空闲GPU不足无法开机，是否继续关机？
Buttons: 取消 / 确定
```

录制中观察到的关机确认动作：

```text
Click AXButton title: 确定
```

2026-06-29 真实关机测试中观察到的工具与弹窗变数：

```text
Computer Use get_app_state 连续超时，无法稳定读取 Chrome。
前台 Chrome 曾是本地 Isaac Lab Monitor 标签，不是 AutoDL。
新开 AutoDL 标签时曾落到登录页，因为当前 profile 不是已登录 AutoDL 的 `皓`。
切到 Chrome `Profile 1` 后，AutoDL 实例列表显示账号 `炼丹师7151`。
```

```text
Chrome 控制读取 DOM snapshot 可稳定看到目标实例行。
目标行点击前:
实例: 内蒙B区 / 094机 2c89409aa0-5dc36344
状态: 运行中
规格: RTX 4090 * 1卡
健康状态: 正常
付费方式: 按量计费
余额提示: 余额不足24小时
操作: 关机 / 更多
分页: 共 1 条
```

```text
关机弹窗在 DOM snapshot 中可见，但 `[role="dialog"]` locator 匹配不到。
可见弹窗文字:
确认关机吗？
实例：2c89409aa0-5dc36344
按量计费的实例关机后GPU将会释放，再次开机时可能面临空闲GPU不足无法开机，是否继续关机？
Buttons: 取消 / 确定

处理方式:
先确认弹窗标题、实例 ID、释放 GPU 提示仍存在。
再确认页面中 `确定` 按钮唯一，才点击 `确定`。
```

```text
点击确定后的过渡态:
状态: 关机中
操作: 开机 / 更多
开机按钮: disabled

这个状态只能说明关机请求已提交，不能汇报最终成功。
```

```text
短等后最终验证状态:
状态: 已关机
资源提示: GPU充足
规格: RTX 4090 * 1卡
付费方式: 按量计费
释放时间/停机时间: 14天23小时59分后释放
操作: 开机 / 更多
分页: 共 1 条
```

后来只读验证中观察到的关机后状态：

```text
实例: 内蒙B区 / 094机 2c89409aa0-5dc36344
状态: 已关机
资源提示: GPU充足
规格: RTX 4090 * 1卡
付费方式: 按量计费
释放时间/停机时间: 14天23小时58分后释放
操作: 开机 / 更多
```

2026-07-02 真实页面状态更新：

```text
可用开机目标:
实例: 内蒙B区 / 058机 578e4b9b8e-b3321bef
状态: 已关机 GPU充足
规格: RTX 4090 * 1卡
健康状态: 正常
付费方式: 按量计费
操作: 开机 / 更多
```

```text
旧 094 机不再作为默认开机目标:
实例: 内蒙B区 / 094机 2c89409aa0-5dc36344
状态: 已关机
规格: RTX 4090 * 1卡
健康状态: 正常
付费方式: 按量计费
操作: 开机 / 更多

点击开机后弹窗:
该主机空闲GPU不足
实例GPU需求数量：1 卡
主机GPU空闲数量：0 卡
```

后来开机试运行中观察到的开机确认弹窗：

```text
确认开机吗？
实例将按￥1.98/时整点扣费
Buttons: 取消 / 确定
```

注意：这个开机弹窗没有显示实例 ID。使用它时必须先在实例列表里锁定唯一目标行，并确认目标行是 `已关机`、`GPU充足`、`按量计费`、`开机`。

2026-06-29 真实开机执行中踩到的坑：

```text
Computer Use 读到的 `开机` element_index 可能在点击时失效。
处理方式不是回放录制坐标，而是在重新确认当前目标行后，
只对当前截图里的目标行 `开机` 按钮做一次坐标兜底点击。
```

2026-06-29 开机确认后观察到的运行态：

```text
实例: 内蒙B区 / 094机 2c89409aa0-5dc36344
状态: 运行中
规格: RTX 4090 * 1卡
健康状态: 正常
付费方式: 按量计费
余额提示: 余额不足24小时
释放时间/停机时间: 关机15天后释放
快捷工具: JupyterLab / AutoPanel / 实例监控 / 自定义服务
操作: 关机 / 更多
```

注意：`余额不足24小时` 可以和 `运行中` 同时出现。若目标行已经是 `运行中` 且操作按钮是 `关机`，应汇报“开机成功，但余额不足24小时”，不能误判成开机失败。

已知变数：

- 浏览器可能有很多窗口和标签页。
- Chrome 可能先显示 profile picker。
- 前台 Chrome 可能是无关标签；不要依赖前台标签就是 AutoDL。
- 当前 Chrome profile 可能没有 AutoDL 登录态；已知可用 profile 是 `皓` / `Profile 1`。
- AutoDL 页面可以从首页、控制台首页或实例列表 URL 进入。
- Computer Use 可能读取 Chrome 超时；可改用 Chrome 控制插件读取 DOM snapshot。
- Computer Use 的 element index 不是稳定选择器，可能需要重新读页面或当前截图坐标兜底。
- AutoDL 确认弹窗可能没有标准 `role="dialog"`；用可见弹窗文字和唯一按钮做兜底。
- 目标行运行时通常显示 `运行中` 和 `关机`。
- 关机后可能先显示 `关机中` 和禁用 `开机`，这是过渡态。
- 目标行停机后通常显示 `已关机`、`GPU充足` 和 `开机`。
- 目标行运行时可能显示 `余额不足24小时`，这是计费风险提示，不是运行失败。
- 页面可能显示 `设置定时关机`；这不是立即开关机按钮。
- `更多` 菜单包含破坏性或无关操作，避免打开。
