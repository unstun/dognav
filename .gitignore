# Live clone of https://github.com/mattpocock/skills.git — pull it for updates,
# don't embed it in this repo. The .claude/skills and .agents/skills symlinks
# point into it. See SKILLS.md.
/.skills-upstream/

# Regenerated by ./sync-skills.sh from the upstream clone above — these are
# relative symlinks into /.skills-upstream/, so committing them would only push
# dangling links. Run ./sync-skills.sh after cloning to recreate them.
/.claude/skills/
/.agents/skills/
