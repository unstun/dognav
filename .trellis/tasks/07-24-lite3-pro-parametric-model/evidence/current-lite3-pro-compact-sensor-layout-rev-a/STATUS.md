# Status

`compact_source_brep_layout_candidate_receiver_contract_open`

The older retained carrier is approximately `153.7 mm` long, while the
photo-measured current-Pro zone between the nose edge and the
uncertainty-expanded compute enclosure is approximately `116 mm`. It therefore
cannot remain wholly inside that zone.

Rev A tests a compact `110 x 115 mm` planning surface. The assembly uses the
official Mid-360 B-rep, the related-source S410 B-rep, and the official D435i
`90 x 25 x 25 mm` datasheet envelope. The layout has no sensor intersection
with the expanded compute-box keep-out, but its smallest fore/aft margins are
only a few millimetres.

The planning surface has zero thickness. No lower carrier solid, robot bore,
STL, screw choice, strength claim, physical fit, or print release exists.
