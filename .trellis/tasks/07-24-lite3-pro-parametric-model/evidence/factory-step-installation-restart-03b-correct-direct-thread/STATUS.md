# Restart step 03B: corrected direct-thread bracket assembly

- Status: `awaiting_visual_review`
- Starting state: the visually accepted J17A/D435i first layer and the
  visually accepted J20A/MID360 upper subassembly remain separate.
- Guard interface: S410's official `4 x diameter 5.2 mm` clearance feet seat
  on J20A's official `4 x M5` threaded axes. Four independent M5x8
  socket-head screws remain visual candidates; no guard nut is shown.
- Layer interface: the complete guarded J20A upper subassembly seats on J17A.
  Two M3x6 and two M4x10 short socket-head display candidates enter from the
  J17A head-seat side, pass through J17A's clearance geometry, and terminate
  inside J20A's modeled threaded receivers.
- Explicit exclusions: the rejected M3x12/M4x18 long through-bolts, all
  far-side nuts, washers, and locknuts, the Lite3 robot, and robot-side
  attachment hardware are absent from every frame.
- Visual diagnostics: yellow rings mark J17A head-seat/clearance axes; blue
  rings mark the corresponding J20A modeled threaded-receiver axes. Only the
  relevant source bodies are ghosted during hidden-path inspection.
- Claim boundary: the video demonstrates the source-CAD fastening direction
  and receiver relationship. M3x6, M4x10, and M5x8 lengths and socket-head
  forms remain display candidates. Factory-supplied hardware, usable thread
  engagement, torque, strength, vibration, exact hand-tool access, and
  real-hardware safety remain unvalidated.
- Validation result: all 480 Fusion-rendered frames are present at
  `1280 x 720`; the H.264 output decodes without error as a 20-second, 24-fps
  video. Fusion occurrence transforms, visibility, opacity, custom graphics,
  and snapshot state were restored after rendering.
- Next gate: Dr Sun visual review of the no-text step-03B video.

